const todoFooterStyles = theme => ({
  listCaption: {
    padding: theme.spacing(1),
    display: 'inline-flex'
  },
  captionCounter: {
    margin: theme.spacing(2)
  },
  captionButton: {
    margin: theme.spacing(1),
    color: theme.palette.secondary.grey
  }
});

export default todoFooterStyles;
