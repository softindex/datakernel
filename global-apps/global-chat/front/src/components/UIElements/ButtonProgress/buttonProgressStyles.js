const buttonProgressStyles = (theme) => ({
  buttonProgress: {
    position: 'absolute',
    top: '50%',
    left: 54,
    marginTop: -12,
    marginLeft: -12
  },
  wrapper: {
    margin: theme.spacing,
    position: 'relative'
  }
});

export default buttonProgressStyles;
