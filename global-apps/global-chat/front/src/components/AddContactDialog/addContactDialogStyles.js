const addContactDialogStyles = theme => ({
  actionButton: {
    margin: theme.spacing,
    position: 'relative',
    right: theme.spacing.unit * 2
  },
  textField: {
    width: theme.spacing.unit * 50
  }
});

export default addContactDialogStyles;
