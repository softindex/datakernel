const contactMenuStyles = theme => ({
  wrapperButton: {
    width: theme.spacing(8)
  },
  menuPaper: {
    maxHeight: theme.spacing(27),
    width: theme.spacing(25)
  }
});

export default contactMenuStyles;
