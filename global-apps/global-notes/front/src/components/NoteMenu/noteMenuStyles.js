const noteMenuStyles = theme => ({
  wrapperButton: {
    minWidth: theme.spacing(8),
    height: theme.spacing(8)
  },
  iconButton: {
    marginTop: theme.spacing(1)
  },
  menuPaper: {
    maxHeight: theme.spacing(27),
    width: theme.spacing(25)
  }
});

export default noteMenuStyles;
