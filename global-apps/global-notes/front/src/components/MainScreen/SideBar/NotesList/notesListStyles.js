const notesListStyles = theme => ({
  notesList: {
    color: theme.palette.grey[600],
    width: '20%',
    height: '100%'
  },
  progressWrapper: {
    marginLeft: theme.spacing.unit * 18.75,
    marginTop: theme.spacing.unit * 1.25
  }
});

export default notesListStyles;
