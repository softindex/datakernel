const inviteDialogStyles = theme => ({
  progressWrapper: {
    margin: theme.spacing(30),
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  }
});

export default inviteDialogStyles;
