{{<base}}
    {{$content}}
        <div class="container list-group">
            {{#threads}}
                <a href="{{pubKey}}/thread/{{id}}" class="list-group-item list-group-item-action">
                    {{meta.title}}
                    <span class="badge badge-secondary badge-light float-right">{{root.author.id}}
                        , {{#format_date}}{{root.initialTimestamp}}{{/format_date}}</span>
                </a>
            {{/threads}}
        </div>
    {{/content}}
{{/base}}
