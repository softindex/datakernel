{{<base}}
    {{$content}}
        <div class="container">
            <div class="card">
                <div class="card-header">
                    <div class="font-weight-bold ml-3">
                        {{thread.title}}
                    </div>
                </div>
                <div class="card-body">
                    {{#post}}
                        {{>render_post}}
                    {{/post}}
                </div>
            </div>
        </div>
    {{/content}}
{{/base}}
