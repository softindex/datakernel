const deleteMenuStyles = theme => ({
  listItemText: {
    paddingRight: theme.spacing(2)
  },
  listItemIcon: {
    minWidth: 0,
    marginRight: theme.spacing(2)
  }
});

export default deleteMenuStyles;
